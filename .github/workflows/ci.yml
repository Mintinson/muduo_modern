name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-26.04, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install GCC 16
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install --yes gcc-16 g++-16

      - name: Configure (Linux, GCC 16)
        if: runner.os == 'Linux'
        env:
          CC: gcc-16
          CXX: g++-16
        run: >-
          cmake -S . -B build
          -DCHAOXI_BUILD_V2=ON
          -DCHAOXI_BUILD_EXAMPLES=ON
          -DCHAOXI_BUILD_TESTS=ON
          -DCHAOXI_BUILD_BENCHMARKS=OFF
          -DCMAKE_BUILD_TYPE=Release

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: >-
          cmake -S . -B build
          -DCHAOXI_BUILD_V2=ON
          -DCHAOXI_BUILD_EXAMPLES=ON
          -DCHAOXI_BUILD_TESTS=ON
          -DCHAOXI_BUILD_BENCHMARKS=OFF
          -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Test
        run: ctest --test-dir build -C Release --output-on-failure
